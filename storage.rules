rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite a cualquier usuario autenticado subir y leer archivos.
    // Ajusta si necesitas más restricciones por ruta.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
